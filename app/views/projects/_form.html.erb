<%= form_for @project do |f| %>
  <p>
    <%= f.label :title, "プロジェクトのタイトル名；" %><br>
    <%= f.text_field :title %>
    <% if @project.errors.any? %>
      <%= @project.errors.messages[:title][0] %>
    <% end %>
  </p>

  <p>
    <%= f.submit "プロジェクトを新規作成" %>
  </p>
<% end %>